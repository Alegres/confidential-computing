apiVersion: v1
kind: Service
metadata:
  labels:
    app: plain-app
  name: plain-service
spec:
  ports:
  - port: 8081
    protocol: TCP
    targetPort: 8081
  selector:
    app: plain-app
  type: ClusterIP
